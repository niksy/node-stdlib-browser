import {
	decode,
	encode,
	escape,
	parse,
	stringify,
	unescape
} from 'native-querystring';

export default {
	decode,
	encode,
	escape,
	parse,
	stringify,
	unescape
};

export { decode, encode, escape, parse, stringify, unescape };
